import React from 'react';

const Settings: React.FC = () => {
    return (
        <div>
            <h1>Settings</h1>
            <p>This is where you can configure application settings.</p>
        </div>
    );
};

export default Settings;